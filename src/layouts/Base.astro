---
// Props: title (string), lang ("es" | "en")
import "../styles/globals.css";
import { getText } from '../utils/texts';
import Footer from '../components/common/Footer.astro';

const {
  title = "Innovados",
  lang = "es",
} = Astro.props;
const isEN = lang === "en";
const t = getText('common', lang);
---
<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | Innovados</title>
    <meta name="description" content={isEN ? "Innovados corporate landing and portfolio" : "Landing corporativa y portafolio de Innovados"} />
    <style>
      /* quick CSS reset */
      *,*::before,*::after{box-sizing:border-box;margin:0;}
    </style>
  </head>
  <body>
    <header>
      <nav class="nav">
        <div class="container nav-inner">
          <a class="logo" href="/">
            <img src="/INNOVADOS_WEB_Logo_Digital_Marketing.png" alt="Innovados logo" height="40" />
          </a>
          <input type="checkbox" id="menu-toggle" hidden />
            <label for="menu-toggle" class="hamburger" aria-label="Menu">
              <span></span><span></span><span></span>
            </label>
            <ul>
            <li><a href={isEN ? "/en/" : "/"}>{isEN ? "Home" : "Inicio"}</a></li>
            <li><a href={isEN ? "/en/services" : "/servicios"}>{isEN ? "Services" : "Servicios"}</a></li>
            <li><a href={isEN ? "/en/portfolio" : "/portafolio"}>{isEN ? "Portfolio" : "Portafolio"}</a></li>
            <li><a href={isEN ? "/en/about" : "/nosotros"}>{isEN ? "About" : "Nosotros"}</a></li>
            <li><a href={isEN ? "/en/blog" : "/blog"}>Blog</a></li>
            <li><a class="cta" href={isEN ? "/en/contact" : "/contacto"}>{isEN ? "Let's Talk" : "Conversemos"}</a></li>
            <li><a class="lang" href={isEN ? "/" : "/en/"}>
              <span aria-hidden="true">{isEN ? "🇪🇸" : "🇺🇸"}</span>
              <span>{isEN ? "ES" : "EN"}</span>
            </a></li>
          </ul>
        </div>
              </nav>
    </header>
    <main>
      <slot />
    </main>
    <Footer lang={lang} isEN={isEN} />
  </body>
</html>
